[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: tests that touch integration boundaries
    live_price: tests that require explicit live read-only price provider access
    db_integration: tests that verify database write/read integration
